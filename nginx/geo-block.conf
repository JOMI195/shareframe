# Denied by country. Allow-by-default: unknown/empty codes pass.
map $geoip2_country_code $allowed_country {
    default yes;

    CN no; RU no; KR no; UA no;

    # sanctioned / hostile states
    BY no; IR no; KP no; SY no; CU no; VE no; AF no; SD no; MM no;

    # high-volume scan/attack origins - Asia
    VN no; IN no; ID no; TH no; PK no; BD no;
    PH no; KH no; LA no; NP no; LK no; MN no;

    # high-volume scan/attack origins - other
    BR no; TR no; NG no; EG no; ZA no; KZ no; UZ no;
    AZ no; AM no; GE no; MD no; IQ no; DZ no; MA no;
    RO no; BG no;

    # offshore / hosting
    SC no; PA no; BZ no; VG no; KY no; GI no;
}
