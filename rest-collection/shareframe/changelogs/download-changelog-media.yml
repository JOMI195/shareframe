info:
  name: download-changelog-media
  type: http
  seq: 3

http:
  method: GET
  url: "{{http_domain}}/api/media/changelogs/<path>"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
