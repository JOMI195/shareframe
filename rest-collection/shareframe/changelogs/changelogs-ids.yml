info:
  name: changelogs-ids
  type: http
  seq: 1

http:
  method: GET
  url: "{{http_domain}}/api/changelogs/ids/"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
