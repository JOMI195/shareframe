info:
  name: sent-image-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{http_domain}}/api/sent-images/1/"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
