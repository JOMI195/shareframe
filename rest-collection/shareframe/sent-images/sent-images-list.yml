info:
  name: sent-images-list
  type: http
  seq: 1

http:
  method: GET
  url: "{{http_domain}}/api/sent-images/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
