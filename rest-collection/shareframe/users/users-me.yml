info:
  name: users-me
  type: http
  seq: 3

http:
  method: GET
  url: "{{http_domain}}/api/auth/users/me/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
