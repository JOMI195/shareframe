info:
  name: user-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{http_domain}}/api/auth/users/1/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
