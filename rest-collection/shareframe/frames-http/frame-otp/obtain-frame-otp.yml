info:
  name: obtain-frame-otp
  type: http
  seq: 1

http:
  method: POST
  url: "{{http_domain}}/api/frames/1/obtain-frame-otp/"
  headers:
    - name: X-CSRFToken
      value: "{{csrf_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
