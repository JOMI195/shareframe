info:
  name: frames-list
  type: http
  seq: 1

http:
  method: GET
  url: "{{http_domain}}/api/frames/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
