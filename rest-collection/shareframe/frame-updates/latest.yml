info:
  name: latest
  type: http
  seq: 2

http:
  method: GET
  url: "{{http_domain}}/api/frame-updates/latest/"
  headers:
    - name: Authorization
      value: Frame-Access-Token {{frame_access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
