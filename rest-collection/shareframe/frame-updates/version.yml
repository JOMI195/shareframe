info:
  name: version
  type: http
  seq: 3

http:
  method: GET
  url: "{{http_domain}}/api/frame-updates/1.0.0"
  headers:
    - name: Authorization
      value: Frame-Access-Token {{frame_access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
