info:
  name: friendship-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{http_domain}}/api/friendships/1/"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
