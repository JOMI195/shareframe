info:
  name: friendship-delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{http_domain}}/api/friendships/1/"
  headers:
    - name: X-CSRFToken
      value: "{{csrf_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
