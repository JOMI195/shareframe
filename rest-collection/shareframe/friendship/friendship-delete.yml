info:
  name: friendship-delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{http_domain}}/api/friendships/1/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
