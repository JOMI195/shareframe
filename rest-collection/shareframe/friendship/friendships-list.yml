info:
  name: friendships-list
  type: http
  seq: 1

http:
  method: GET
  url: "{{http_domain}}/api/friendships/"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
