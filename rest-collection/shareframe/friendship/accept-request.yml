info:
  name: accept-request
  type: http
  seq: 5

http:
  method: POST
  url: "{{http_domain}}/api/friendships/1/accept-request/"
  headers:
    - name: X-CSRFToken
      value: "{{csrf_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
