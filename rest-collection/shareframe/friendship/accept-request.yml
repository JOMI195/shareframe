info:
  name: accept-request
  type: http
  seq: 5

http:
  method: POST
  url: "{{http_domain}}/api/friendships/1/accept-request/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
