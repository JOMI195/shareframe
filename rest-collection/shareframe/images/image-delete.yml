info:
  name: image-delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{http_domain}}/api/images/1/"
  headers:
    - name: X-CSRFToken
      value: "{{csrf_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
