info:
  name: statistics
  type: http
  seq: 1

http:
  method: GET
  url: "{{http_domain}}/api/dashboard/statistics/"
  auth:
    type: bearer
    token: "{{env_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
