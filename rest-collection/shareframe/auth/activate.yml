info:
  name: activate
  type: http
  seq: 6

http:
  method: POST
  url: "{{http_domain}}/api/auth/users/activation/"
  headers:
    - name: X-CSRFToken
      value: "{{csrf_token}}"
  body:
    type: json
    data: |-
      {
          "uid": "<uid-from-email>",
          "token": "<token-from-email>"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
