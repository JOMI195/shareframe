info:
  name: jwt-verify
  type: http
  seq: 3

http:
  method: POST
  url: "{{http_domain}}/api/auth/jwt/verify/"
  body:
    type: json
    data: |-
      {
          "token": "{{env_auth_token}}"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
