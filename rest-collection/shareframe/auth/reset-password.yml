info:
  name: reset-password
  type: http
  seq: 9

http:
  method: POST
  url: "{{http_domain}}/api/auth/users/reset_password/"
  headers:
    - name: X-CSRFToken
      value: "{{csrf_token}}"
  body:
    type: json
    data: |-
      {
          "email": "{{backend_admin_email}}"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
