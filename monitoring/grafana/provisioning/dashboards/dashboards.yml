apiVersion: 1

providers:
  - name: shareframe
    orgId: 1
    folder: ShareFrame
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
