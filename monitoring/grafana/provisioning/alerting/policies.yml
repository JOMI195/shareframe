apiVersion: 1

# Replaces the default tree, whose root receiver is Grafana's example address.
policies:
  - orgId: 1
    receiver: shareframe-admin
    group_by: [grafana_folder, alertname, userID, clientIP]
    group_wait: 10s
    group_interval: 1m
    repeat_interval: 4h
